import React from "react";

const Home = () => {
    return(
        <div>
            <h1>Home Page</h1>
            <h2>React Lifecycle Methods - Class Components</h2>
            <h6>In class components, React provides special methods called lifecycle methods that run at
specific points in a component’s life:
1. Mounting → When the component is first added to the DOM.
2. Updating → When the component re-renders (because props or state changed).
3. Unmounting → When the component is removed from the DOM.</h6>
        </div>
    )
}

export default Home